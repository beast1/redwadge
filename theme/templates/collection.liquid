{% include 'page-title' %}

<div class="page-main">
  <div class="catalog">
    <div class="catalog__controls">
      
    </div>
    <div class="catalog__list">
    
      {% for product in products limit: 12 %}
        {% include 'card' %}
      {% endfor %}
    
    </div>
    
    {% include 'pagination' %}
    
  </div>
  
  {% include 'text-field' %}
  
</div>