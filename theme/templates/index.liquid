{% include 'alert' %}

<div class="page-main">
  {% include 'carousel' %}
  {% include 'features' %}
  
  <div class="blog">
    <h2 class="blog__title">Новости</h2>
    <div class="blog-list">
        {% for article in blogs.news.articles limit: 4 %}
          {% include 'blog-demo' %}
        {% endfor %}
    </div>
  </div>
  
  {% include 'text-field' %}
  
</div>